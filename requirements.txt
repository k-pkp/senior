torch>=2.5.1
torchvision>=0.20.1
pytorch-lightning>=2.2.1
diffusers>=0.27.2
transformers>=4.40.0
accelerate>=0.29.3
numpy>=1.26.4
open3d>=0.18.0
trimesh>=4.3.2
xatlas>=0.0.9
pyrender>=0.1.45
scipy>=1.12.0
opencv-python-headless>=4.9.0.80
Pillow>=10.3.0
pillow-heif>=0.16.0
segment_anything>=1.0
rembg>=2.0.56
huggingface_hub
einops
safetensors
omegaconf
tqdm
pymeshfix
pyvista
pandas
plotly>=5.0.0
scikit-learn>=1.3.0
psutil>=5.9.0
alphashape>=1.3.1
shapely>=2.0.0

# ── GPU acceleration (optional) ───────────────────────────────────────────────
# Stage 3 DBSCAN, KNN, and outlier removal run on GPU if cupy + cuml installed.
# All ops fall back to CPU automatically when these are absent.
#
# Pick ONE cupy line matching your CUDA version, then install cuml:
#
#   CUDA 11.x:
#     pip install cupy-cuda11x
#     pip install cuml-cu11 --extra-index-url=https://pypi.nvidia.com
#
#   CUDA 12.x:
#     pip install cupy-cuda12x
#     pip install cuml-cu12 --extra-index-url=https://pypi.nvidia.com
#
#   CUDA 13.x (cuml-cu12 via backward compat):
#     pip install cupy-cuda13x
#     pip install cuml-cu12 --extra-index-url=https://pypi.nvidia.com
#
# Stage 7 GPU voxelization (separate optional dep):
#   pip install warp-lang>=1.3.0